description: A list of coupons
content:
  application/json:
    schema:
      type: object
      properties:
        metadata:
          $ref: '../../schemas/MetaData.yaml'
        data:
          type: array
          items:
            $ref: '../../schemas/inventory/coupon/Coupon.yaml'
