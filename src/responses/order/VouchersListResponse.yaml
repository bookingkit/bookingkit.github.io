description: A list of vouchers
content:
  application/json:
    schema:
      type: object
      properties:
        metadata:
          $ref: '../../schemas/MetaData.yaml'
        data:
          type: array
          items:
            $ref: '../../schemas/order/voucher/Voucher.yaml'
