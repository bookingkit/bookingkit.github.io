description: A list of features you have access to
content:
  application/json:
    schema:
      type: object
      properties:
        metadata:
          $ref: '../schemas/MetaData.yaml'
        data:
          type: array
          items:
            $ref: '../schemas/features/Feature.yaml'
