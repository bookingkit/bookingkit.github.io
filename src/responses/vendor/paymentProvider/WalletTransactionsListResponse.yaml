description: list of wallet transactions
content:
  application/json:
    schema:
      type: object
      properties:
        metadata:
          $ref: '../../../schemas/MetaData.yaml'
        data:
          type: array
          items:
            $ref: '../../../schemas/vendor/paymentProvider/WalletTransaction.yaml'
