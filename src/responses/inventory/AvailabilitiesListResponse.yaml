description: A list of availabilities
content:
  application/json:
    schema:
      type: object
      properties:
        metadata:
          $ref: '../../schemas/MetaData.yaml'
        data:
          type: array
          items:
            $ref: '../../schemas/inventory/Availability.yaml'
