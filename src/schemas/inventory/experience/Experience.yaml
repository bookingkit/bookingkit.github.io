allOf:
  - type: object
    required:
      - id
      - uri
    properties:
      id:
        $ref: 'fields/ExperienceId.yaml'
      uri:
        $ref: '../../dataObjects/Uri.yaml'
  - $ref: 'ExperienceProperties.yaml'
