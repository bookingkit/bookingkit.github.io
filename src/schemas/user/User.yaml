allOf:
  - type: object
    required:
      - id
      - uri
    properties:
      id:
        $ref: 'fields/UserId.yaml'
      uri:
        $ref: '../dataObjects/Uri.yaml'
  - $ref: 'UserProperties.yaml'
