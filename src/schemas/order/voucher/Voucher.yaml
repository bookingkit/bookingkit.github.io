allOf:
  - type: object
    required:
      - id
      - uri
    properties:
      id:
        $ref: 'fields/VoucherId.yaml'
      uri:
        $ref: '../../dataObjects/Uri.yaml'
  - $ref: 'VoucherProperties.yaml'
