allOf:
  - type: object
    required:
      - id
      - uri
    properties:
      id:
        $ref: 'fields/VendorId.yaml'
      uri:
        $ref: '../dataObjects/Uri.yaml'
  - $ref: 'VendorProperties.yaml'
