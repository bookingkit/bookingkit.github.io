parameters:
  - $ref: '../../parameters/UserIdParam.yaml'
  - $ref: '../../parameters/resellerId.yaml'
summary: User Reseller access | remove
description: removes the access for this user for a reseller
security:
  - OAuth2:
      - user_write
  - OAuth2:
      - global_user_write
responses:
  $ref: '../../responseTypes/DelayedDeletionEndpointResponses.yaml'

