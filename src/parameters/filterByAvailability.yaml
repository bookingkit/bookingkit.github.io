name: 'units'
in: query
description: amount of required available tickets
required: false
style: form
explode: false
schema:
  type: array
  items:
    type: object
    required:
      - id
      - quantity
    properties:
      id:
        type: string
        format: string
      quantity:
        type: integer
        format: int32
