name: 'localEndTime'
in: query
description: |
  The latest local time acceptable, the provided time is NOT included (<)
  this filter is specifically for the local time of the service
required: false
schema:
  type: string
  format: date-time
